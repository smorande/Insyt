version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  runtime-version: 3.9.16
  command: streamlit run app66d.py --server.port=8080 --server.address=0.0.0.0
  network:
    port: 8080
  env:
    - name: PORT
      value: "8080"